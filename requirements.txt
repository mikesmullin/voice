# Core dependencies (Piper TTS, no GPU required)
pyyaml>=6.0
soundfile>=0.12.1
sounddevice>=0.4.6
piper-tts>=1.3.0

# Optional: Kokoro TTS (macOS / CPU-only)
# pip install torch kokoro
#
# Optional: Kokoro TTS (Linux/Windows with NVIDIA CUDA 12.x)
# pip install --extra-index-url https://download.pytorch.org/whl/cu129 torch torchaudio kokoro


