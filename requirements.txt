# Core dependencies
pyyaml>=6.0
soundfile>=0.12.1
sounddevice>=0.4.6

# PyTorch with CUDA support
# Using cu129 to match CUDA 12.9
--extra-index-url https://download.pytorch.org/whl/cu129
torch>=2.0.0
torchaudio>=2.0.0

# Kokoro TTS
kokoro>=0.9.2

# StyleTTS2 dependencies (optional, only needed if using StyleTTS2 model)
munch>=2.5.0
librosa>=0.10.0
nltk>=3.8
phonemizer>=3.2.0
accelerate>=0.20.0
transformers>=4.30.0
einops>=0.6.0
einops-exts>=0.0.4
tqdm>=4.65.0
matplotlib>=3.7.0
git+https://github.com/resemble-ai/monotonic_align.git

